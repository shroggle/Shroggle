function showPageVersionDescriptionMoreInfo() {
    var showWindow = createConfigureWindow({width: 700, height: 400});
    showWindow.setContent($("#pageVersionDescriptionMoreInfoText").html());
}

function showPageVersionUrlInfo() {
    var showWindow = createConfigureWindow({width: 700, height: 400});
    showWindow.setContent($("#pageVersionUrlInfoText").html());
}

function showPageTitleInfo() {
    var showWindow = createConfigureWindow({width: 700, height: 400});
    showWindow.setContent($("#pageTitleInfoText").html());
}

function showPageVersionKeywordsMoreInfo() {
    var showWindow = createConfigureWindow({width: 700, height: 400});
    showWindow.setContent($("#pageVersionKeywordsMoreInfoText").html());
}

function showHoToSetUpDomainName() {
    var helpWindow = createConfigureWindow({width: 700, height: 400});
    helpWindow.setContent($("#ownDomainName").html());
}